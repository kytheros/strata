/** Community edition stub — never called since getCachedGeminiProvider returns null. */
export async function enhancedExtract(_session: unknown, _provider: unknown): Promise<never> {
  throw new Error("Enhanced extraction requires Strata Pro");
}
