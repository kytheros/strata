/** Community edition stub — never called since getCachedGeminiProvider returns null. */
export async function smartSummarize(_session: unknown, _provider: unknown): Promise<never> {
  throw new Error("Smart summarization requires Strata Pro");
}
